# API Documentation

## Functions

### hello_world

Return a hello world string

**Returns:** String greeting from the C extension

**Example:**

```python
import example_module
result = example_module.hello_world()
print(result)  # Output: "Hello from C extension!"
```

### add_numbers

Add two integers

**Parameters:**

- `a` (int): First integer
- `b` (int): Second integer

**Returns:** Sum of the two integers

**Example:**

```python
import example_module
result = example_module.add_numbers(5, 3)
print(result)  # Output: 8
```

**Error Handling:**

- Raises `TypeError` if arguments are not integers
- Raises `TypeError` if incorrect number of arguments provided

---

## Notes

This documentation is automatically generated by the CI/CD pipeline.
